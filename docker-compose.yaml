version: '3.8'

services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      MONGODB_USER: jsharmaishere
      MONGODB_PASSWORD: KCXRcwocxBBgKsWR
      MONGODB_HOST: cluster0.b4ad5xn.mongodb.net
